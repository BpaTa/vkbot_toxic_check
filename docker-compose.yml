services:
  bot:
    container_name: vk_bot_toxic_check
    build: ./
    image: bot-toxic
    depends_on:
      - db
    command: sh -c "sleep 60; python3 ./main.py"
    env_file: .env

  db:
    image: postgres:13-alpine
    restart: always
    env_file: .env
    ports:
      - '5432:5432'